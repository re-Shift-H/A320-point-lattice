# Core generator (generate_lattice.py)
numpy>=1.24
pandas>=2.0
openpyxl>=3.1

# Optional: preview render (preview.py)
matplotlib>=3.7

# Optional: CATIA automation (build_in_catia.py), Windows only
pywin32>=306
